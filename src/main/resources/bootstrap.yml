server:
  servlet:    context-path: /user1
  port: 8086
  

  
spring:
  application:    name: user-service-1
  profiles:
    active: development
  cloud:
    config:
      enabled: true
      uri: http://localhost:8085
      
      
    
  